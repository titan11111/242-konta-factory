# SPEC — 242｜コン太と夕焼け廃工場

- 作成日: 2026-09-19 ／ 最終更新: 2026-09-21
- フォルダ: `242-konta-factory`
- ジャンル: 横スクロールアクション（全5ステージ）
- 想定プレイ時間: 通し1回 5〜8分

## 1. コアループ（何が面白いか）

ヤリと**2機のドローン**で機械兵を倒しながら、廃工場の5区画を奥へ進む。

- **守りドローン**（シールド）: 被弾を1回だけ肩代わりする。切れたら480フレームかけて再チャージ
- **攻めドローン**: 自律行動。敵の頭上へ移動して溜め、真下へビームを落とす（移動1.17px/f＝プレイヤーの約半分）
- **ヤリのため技**: 攻撃ボタンを押し続けて離すと衝撃波が出る

「シールドを盾に前へ出る／切れたら溜めて待つ」の判断がそのままスコアになる。

## 2. 操作

| 入力 | キー | 画面ボタン |
|---|---|---|
| 移動 | ← → | 左下 ◀ ▶ |
| ジャンプ | Space / ↑ | 右下「ジャンプ」 |
| ヤリ攻撃 | X / J | 右下「ヤリ」 |
| **ため技** | X / J を長押しして離す | 「ヤリ」を長押しして離す |
| 一時停止 | P / Esc | 画面右上の一時停止ボタン（プレイ中のみ表示） |

すべて Pointer Events（`setPointerCapture`）。指がボタン枠外に流れても取りこぼさない。

### ため技の段（245-raiken-hikari のホールド式を横スクロール用に作り直したもの）

245は「離した瞬間に初めて撃つ」型だが、242はタップの打感が売りなので二段構えにした。

| 段 | 条件 | 効果 |
|---|---|---|
| 通常ヤリ | 押した**瞬間** | 既存どおり即発生。打感を落とさない |
| ため2 | 34フレーム以上ホールドして離す | 貫通する衝撃波（2ダメージ） |
| ため3 (MAX) | 82フレーム以上ホールドして離す | 大型の雷撃波（3ダメージ・画面フラッシュ） |

**エフェクト**: 三日月の刃を残像9枚で引き、外側グロー＋白熱コアの3層構成。MAXは刃の内側を放電が走り、
発生直後11フレームは放射状の飛散が開いて消える。進行方向の後ろへ速度線と火花を撒く。

ゲージはHUD中央に表示。段の境目に目盛りを入れている。

### コン太の歩き（2026-09-21）

接地して移動中、歩幅に同期した gait で動かす。

- **足**: トコトコ。片足ずつ持ち上げ、上げた足は外側へ踏み出す（胴体の下に隠れない）
- **尻尾**: パタパタ。歩き中は gait に合わせて上下へ大きく振る。停止中は小さく、空中は流す
- エンディングの背中歩きも同じリズムに合わせている

## 3. ステージ構成（全5面）

| # | 名前 | 舞台 | 長さ | 主な敵 |
|---|---|---|---|---|
| 1 | 外周 | 夕焼けの廃工場の外壁 | 2600px | 歯車・コウモリ・タレット・這行機・跳ね機・**プロトタイプ００４号** |
| 2 | 工場の中 | ベルトと圧搾機が止まらない内部 | 2800px | ローラー・積層機・突進機・レーザー柱・スパイク・天井プレス・**警備ロボ１００５** |
| 3 | 地下 人体実験場 | 培養槽の並ぶ回廊 | 2800px | 群れ虫・放電機・ドリル・自爆機・分裂機・機雷・心拍シャッター・**模型３号** |
| 4 | 工場奥の空き地 | 捨てられた機体の山 | 2900px | 追跡犬・ブロック兵・高速飛行機・跳躍兵・回転刃・磁力機・崩れる足場・廃材落下・**人造人間の集合体** |
| 5 | 最新研究室 | まだ温かい実験機 | 3000px | 浮遊球・浮遊砲台・監視塔・狙撃機・迫撃砲・スキャンライト・床シャットダウン・**開発者騎乗機** |

- ステージ間でHPを1回復する（通しで遊べる長さにするため）
- 各ステージにチェックポイント2箇所。穴に落ちると直前のチェックポイントへ
- 5面のゲートは**ボス撃破まで閉じている**。倒すと扉が点灯し、通れる
- **1面の空はマジックアワー**（藍→紫→朱→金へ徐々に変わる縦グラデ。低い太陽、逆光の工場）。段差の帯にはしない

### 2面の地形ギミック（2026-09-21）

1面は触らない。2面から地形で「ベルトと圧搾機」を実装する。

| ギミック | 中身 | 置き制約 |
|---|---|---|
| **コンベア床** | 接地中だけ横に流す（+1.1 / −1.1 px/f）。ジャンプ中は効かない。逆流でも歩き（2.4）で押し切れる | 穴の踏み切り帯（穴左110px〜右30px）には置かない |
| **天井プレス** (`press`) | 壊せない。192f周期。予告24f（床が点滅）→叩き下ろし16f→保持36f→格納。開いているあいだに通る。シールドで1回だけ肩代わりできる | 足場・踏み切り帯と x が重ならない |

操作ボタンは増やしていない。

### 3面の地形ギミック（2026-09-21）

| ギミック | 中身 | 置き制約 |
|---|---|---|
| **培養液** | 接地中だけ歩行 1.15px/f（通常2.4）、ジャンプ vy=-6（通常-9）。ダメージはない | 穴の踏み切り帯には置かない。ピット越えは乾いた床から |
| **心拍シャッター** (`shutter`) | 壊せない扉。108f周期で開閉。閉じているあいだは壁。接触ダメージはなし | 足場・踏み切り帯と x が重ならない |

### 4面の地形ギミック（2026-09-21）

| ギミック | 中身 | 置き制約 |
|---|---|---|
| **崩れる足場** | 既存足場4枚。接地22fで崩落、200f後に復帰。揺れ→落下の予告。ダメージなし | 穴の踏み切り帯には置かない。下は床なので落下しても穴には落ちない |
| **廃材落下** (`scrap`) | 壊せない。180f周期。影の予告→落下中だけ hazard。着地後・待機中は当たらない | 穴の踏み切り帯には置かない |

操作ボタンは増やしていない。

### 5面の地形ギミック（2026-09-21）

| ギミック | 中身 | 置き制約 |
|---|---|---|
| **スキャンライト** | 縦の走査光が往復。足場の**下**に隠れると見つからない。光に22f晒されると下向き弾。発見中は sniper / warden / rider のクールダウンが速い | 光は動く。足場の上ではなく下に隠れる |
| **床シャットダウン** | 床に48pxの穴を開け、12pxの格子が180f周期で消える（点灯100f／消灯80f）。消灯前20fは点滅。必要距離64px＜ジャンプ86.4px | 穴の踏み切り帯・既存足場と x が重ならない |

### ボスと扉（2026-09-21）

各面の終端にボス1体。倒すまでゲートは暗い錠前のまま。導入画面にボス名を出す。見た目と当たり判定は描画3倍（`w`/`h` も3倍）。

| 面 | 名前 | 型 | 見た目と攻撃 | 音 |
|---|---|---|---|---|
| 1 | プロトタイプ００４号 | `proto004` hp5 | 片耳の未完成コン太。飛び込み＋1発 | ウイーン |
| 2 | 警備ロボ１００５ | `guard1005` hp6 | 黄黒の盾。近距離は突進、遠距離は弾 | がしゃん |
| 3 | ロボ人体の模型３号 | `model3` hp7 | 骨格と拍動する心臓。弧を描く腕弾 | ピピピ |
| 4 | 人造人間の集合体 | `aggregate` hp8 | 胴が三つ重なる。低HPで mini が剥がれる | がしゃんがしゃん |
| 5 | 開発者騎乗機 | `rider` hp10 | 監督機の胴に人が乗っている。扇状弾 | ウイーン（低） |

## 4. 敵キャラ（定義テーブル）— `enemies.js`

**class は使わない。定義テーブル方式**。1体に必要なのは `update(e)` と `draw(e,x,y,fl)` だけで、
`new`/`this`/継承を持ち込むと本体より定義のほうが長くなるため。敵を増やす作業は
「`ET` に1エントリ足す」に収束する。

`index.html` より先に classic script として読み込む（同一グローバルスコープなので
`p` `GY` `burst()` などをそのまま使え、依存注入が要らない）。

1エントリの形:

| キー | 役割 |
|---|---|
| `hp,w,h` | 初期HPと当たり判定（必須） |
| `ground` | 接地させて配置する |
| `tough` | 一切ダメージが通らない（破壊不可の罠） |
| `spawn(e)` | 配置時の初期化 |
| `update(e)` | 毎フレーム |
| `hazard(e)` | 体当たりとは別の攻撃判定（レーザー・爆風・電撃）を rect で返す |
| `guard(e,fx)` | その方向からの攻撃を防ぐ（盾持ち） |
| `block` | 壁として扱う（接触ダメージなし。心拍シャッター） |
| `drop` | 本体の体当たりをしない（落下物。hazard だけが当たる） |
| `boss` | 撃破でゲートが開く |
| `onDeath(e)` | 撃破時（分裂・爆発） |
| `draw(e,x,y,fl)` | 描画（必須） |

定義キー: 既存30種 + `press` `shutter` `scrap` `proto004` `guard1005` `model3` `aggregate` `rider`
（`mini` は splitter / 集合体の撃破過程でのみ湧く。`overseer` は型として残し、5面の配置は `rider` に交代）

## 5. 画面構成

- **タイトル**: パンク／ディストピア基調。黄黒のハザード帯・走査線・ノイズ・RGBずらしのグリッチ。
  **ゲーム内容の説明文は置かない**（操作説明・ドローン説明は削除済み）。出すのは点滅する進入表示とベストスコアだけ
- **ステージ導入**: `STAGE n / 5` ＋ 区画名・副題。タップで進入
- **プレイ**: HPハート／守り・攻めドローンの状態／ため技ゲージ／区画名／進行度バー／ステージ進捗ドット
- **一時停止**: プレイ中のみ右上ボタン（タイトル・エンディングでは非表示）
- **ゲームオーバー**: 「機能停止」
- **エンディング**: 白フラッシュ → 夕焼けの外 → 3行のテキストが順に出る → スコア板 → タップで再挑戦。
  コン太と2機のドローンが夕日の前を横切って画面外へ歩いていく

## 6. 技術・iOS対応

- `index.html` ＋ `enemies.js` の2本（外部依存はGoogle FontsのCSSのみ、画像・音声ファイル0本・約50KB）
- `#game-shell` を `grid-template-rows: minmax(0,1fr) clamp(132px,25vh,210px)` で分割
- Canvas 論理480×270（16:9）を `devicePixelRatio`（上限2）でバッキングストアへ反映し `image-rendering:pixelated`
- viewport-fit=cover ＋ `env(safe-area-inset-*)` ＋ ダブルタップズーム防止一式
- ボタンは最小72×64px、押下時 `navigator.vibrate`（ため技MAXは振動パターンを変える）。ミュート／一時停止は右上48px
- 明示ポーズ＋`visibilitychange` による自動停止
- `localStorage` キーは `tg.242.best` と `tg.242.mute`

## 7. レベル設計の制約（実測値。破ると詰む）

ジャンプの実測到達距離は **86.4px**（`vy=-9` → 可変ジャンプ補正後 -8.5、重力0.5、滞空36フレーム、水平2.4px/f）。

1. **穴の幅は54pxに統一**。必要距離は「穴幅＋プレイヤー幅16」なので、54なら70px必要＝余裕16px
2. **穴の踏み切り帯（穴の左110px〜右30px）の上に足場を置かない**。
   頂点でのプレイヤー上端は y≈126 なので、この帯に底面 y+12 > 126 の足場があると**頭がつかえて落ちる**

※ 2はこのゲームで実際に**15箇所すべて**に発生していた（下の「検証」参照）

## 8. 音声（2026-09-21）

WebAudioの合成SEのみ（音声ファイル0本）。iOSはジェスチャで `AudioContext.resume()`。失敗してもゲームは止まない。

| 動作 | 音 |
|---|---|
| 歩き | トコトコ（接地中・9fおき、左右で高さを変える） |
| ジャンプ | ボーン |
| ヤリ（押した瞬間） | えい |
| ボス出現・攻撃 | 004／騎乗機＝ウイーン、1005＝がしゃん、模型3号＝ピピピ、集合体＝がしゃんがしゃん |

ミュートボタンは画面右上（一時停止の左）。`localStorage` キーは `tg.242.mute`（`'1'` で消音）。

## 9. 未実装・今後

- 周回・難易度選択
- 30種のうち `mini` 以外は各ステージ固定配置。ランダム配置や湧きは無し

## 10. 検証（2026-09-20 実測）

| 項目 | 方法 | 結果 |
|---|---|---|
| 敵30種 | 各型を1体ずつ260フレーム動かし `update`／`hazard`／`draw`（通常・被弾）を実行 | 30/30 例外なし |
| 画面遷移 | `title → intro → play` を実入力で通す | 到達 |
| ため技3段 | ホールド長を変えて発射結果を取得 | tap=波なし / 34f=lv2(2dmg,w34) / 82f=lv3(3dmg,w56)。押した瞬間の通常ヤリは3ケースとも発生 |
| 攻撃ドローン | 歯車1体だけを置いて900フレーム | 移動**1.17px/f**（2026-09-20に1.3倍へ調整）、敵中心x=150に対しx=146.9で発射、2発で撃破 |
| 5ステージ走破 | 「右を押しっぱなし＋穴と壁で跳ぶ」だけのボットで通し | 5/5 ゴール到達・**落下0回** |
| 背景5種＋エンディング＋タイトル | 全画面を実描画 | 例外0 |
| harness | `_tools/game-harness.sh 242-konta-factory` | 14項目 PASS（コンソールエラー0／0.05MB） |

ジャンプ到達距離86.4pxは推定ではなく、平地で助走→踏み切り→着地までを実測した値。
