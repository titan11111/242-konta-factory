# SPEC — 242｜コン太と夕焼け廃工場

- 作成日: 2026-09-19
- フォルダ: `242-konta-factory`
- ジャンル: 横スクロールアクション
- 想定プレイ時間: 1回1〜2分（クリアまで約1000m）

## 1. コアループ（何が面白いか）

ヤリで機械兵を倒しながら、廃工場のゲートまで一直線に走る。ドローンのシールドが1回だけダメージを肩代わりしてくれるので、「シールドを盾に前に出る／シールドが切れたら慎重に」の判断がそのままスコアに跳ね返る。

## 2. 操作

| 入力 | キー | 画面ボタン |
|---|---|---|
| 移動 | ← → | 左下 ◀ ▶ |
| ジャンプ | Space / ↑ | 右下「ジャンプ」 |
| ヤリ攻撃 | X / J | 右下「ヤリ」 |
| 一時停止 | P / Esc | 画面右上の一時停止ボタン |

すべて Pointer Events（`setPointerCapture`）で実装。指がボタン枠外に流れても入力を取りこぼさない。

## 3. ルール

- HP3。被弾時、シールドがあれば1回だけ無効化（以後480フレームかけて再チャージ）、無ければHP-1
- 敵は3種（歯車＝往復移動、コウモリ＝追尾ホバー、タレット＝射撃）
- チェックポイント3箇所。穴に落ちると直前のチェックポイントへ復帰
- ゲート到達でクリア。スコア＝撃破数×100＋タイムボーナス＋残HP×300
- ベストスコアは `localStorage`（キー `tg.242.best`）に保存

## 4. 画面構成

- タイトル：canvas内に描画（タイトル／操作ヒント／ベストスコア）。タップまたはキーでスタート
- プレイ：HPハート・シールド状態・撃破数・進行度バーをcanvas内HUDで表示
- 一時停止：右上ボタンでいつでもON/OFF。タップで再開
- ゲームオーバー／クリア：撃破数・スコア・タイムをcanvas内に表示、タップでリトライ

## 5. 技術・iOS対応

- 単一 `index.html`（外部依存はGoogle FontsのCSSのみ、画像・音声ファイル0本）
- `#game-shell` を `grid-template-rows: minmax(0,1fr) clamp(132px,25vh,210px)` で分割し、上段をゲーム画面、下段（画面高の約25%）を操作盤に固定
- Canvas 論理480×270（16:9）を `devicePixelRatio`（上限2）でバッキングストアへ反映し `image-rendering:pixelated` でドット感を維持
- viewport-fit=cover ＋ `env(safe-area-inset-*)` ＋ ダブルタップズーム防止一式（`touch-action`／`gesturestart`／`touchend`300ms抑止）
- ボタンは最小72×64px（48px基準を満たす）、押下時 `navigator.vibrate(12)` で触覚フィードバック
- 明示ポーズを実装（`visibilitychange` によるバックグラウンド自動停止も併用）

## 6. 音声について（未実装・意図的）

このゲームは元々WebAudioを一切使用していない（無音設計）。ミュートトグルは「鳴っている音を消す」ためのUIであり、音源が存在しない状態で追加するのは今回のスコープ外と判断し見送った。将来BGM/SEを追加する際は、追加と同時にミュートトグルを必ず実装すること。

## 7. 未実装・今後

- BGM / 効果音（上記の理由により現状ゼロ）
- 難易度カーブ（現状は固定配置の敵のみ、周回要素なし）
