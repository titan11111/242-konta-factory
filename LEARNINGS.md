# LEARNINGS — 242｜コン太と夕焼け廃工場

- 日付: 2026-09-19
- 位置づけ: `242-day066`（本文のみ存在・タイトル未確定・iOS操作盤未実装）を、タイトル確定＋コントロールパネル実装＋公開まで仕上げる作業

## 何をしたか

- フォルダ名を `242-day066` → `242-konta-factory` に変更（タイトル「コン太と夕焼け廃工場」に基づく）。メインファイルを `konta-factory.html` → `index.html` にリネーム（harness / publish.sh の必須要件）
- `#game-shell` / `#game-stage` / `#screen-wrap` / `#control-deck` 構造を導入し、画面下25%を操作盤として固定（`279-haisen-dash` の実装パターンを踏襲）
- 方向ボタン・アクションボタンを `touchstart`+`mousedown` の二重バインドから Pointer Events（`setPointerCapture`）へ統一
- 一時停止ボタンを新設（既存にはゼロだった）。`visibilitychange` によるバックグラウンド自動停止も追加
- `localStorage` キーを `konta-best` → `tg.242.best` に変更（同一オリジン上の他ゲームとの衝突を避ける命名規約に合わせた。未公開のため移行処理は不要と判断）
- Canvas を `devicePixelRatio`（上限2）でバッキングストア化し、`data-logical-width/height` を付与（harnessの「Canvas上段フィット」判定に必要）
- ダブルタップズーム防止一式（`touch-action` / `gesturestart` / `touchend`300ms抑止 / `selectstart` / `dragstart`）を追加
- ゲームロジック本体（移動・当たり判定・敵AI・描画）は無改造

## 設計上の判断

- 音声（BGM/SE）は元々ゼロだったため、ミュートトグルは追加しなかった。「音が鳴るのに消せない」問題への対処であって、無音のゲームにミュートUIだけ足すのは頼まれていない機能追加になると判断（SPEC.md に明記）
- タイトル画面・操作ヒントは元々DOM（`<h1>`+`<p class="help">`）だったが、canvas内描画に統合。理由：`#game-stage` の常時レイアウト面積を確保し、harnessの「Canvas上段フィット」（占有率90%以上）を素直に満たすため

## 検証（証跡）

### `_tools/game-harness.mjs`

```
PASS  index.html: 存在
PASS  モバイル幅: 390px / viewport 390px
PASS  Canvas: 960×540
PASS  描画ループ: 61 RAF/秒
PASS  タップ: イベント送信成功
PASS  コンソールエラー: 0件
PASS  リクエスト失敗: 0件
PASS  通信量20MB以下: 0.02MB（上限20.00MB）
PASS  iOS領域75/25: 操作盤 24.9%
PASS  ゲームと操作盤の非重複: 境界分離
PASS  操作盤内に収まる: deck=fit / viewport=fit
PASS  操作ボタン48px以上: 全ボタン適合
PASS  Canvas上段フィット: 0.78× / 0.78×・占有率 95.9%
PASS  操作盤表示・safe-area余白: 表示=yes / 下余白=10px
RESULT PASS
```

レポート: `docs/harness-reports/242-konta-factory-2026-09-19T07-09-22-804Z.md`

### 手動プレイテスト（Claude Browser / 375×812 モバイルエミュレーション）

- タイトル→タップでプレイ開始を確認
- 右移動ボタン押下でコン太が実際に右へ移動し、進行度バーが伸びることを確認
- ジャンプボタン→足場に着地、ヤリボタン→攻撃モーションが出ることを確認
- 一時停止ボタン→「一時停止中／タップして再開」オーバーレイが表示、再度押下で復帰することを確認

## 未検証・正直に残すこと

- **iPhone実機では未確認**（検証はharnessのPlaywright/WebKitとClaude Browserのエミュレーションのみ）
- 音声を今後追加する場合の設計（現状は完全無音のまま）
- 敵配置・難易度バランスは元実装のまま、今回は未調整

## 2026-09-19 公開（GitHub Pages）
- URL: https://titan11111.github.io/242-konta-factory/ （HTTP 200・Pages status=built を実測）
- publish.sh が OGP タグを index.html へ挿入したため、**公開実体で harness を取り直した**: `docs/harness-reports/242-konta-factory-2026-09-19T07-20-19-018Z.md` → 14項目すべて PASS
- 学び: publish.sh の OGP 挿入は harness の後に走る。公開後の実体で1回取り直さないと、証跡が公開物と一致しない
- 未検証: iPhone実機（harness は Playwright/WebKit 390px のみ）

## 2026-09-19 旧URLの404を修復
- 症状: `https://titan11111.github.io/242-konta-factory/konta-factory.html` が **404**。本体（`/242-konta-factory/`）は 200 で生きていた
- 原因: エントリを `konta-factory.html` → `index.html` へ改名したため、**改名前に配ったリンクだけが死んだ**。リポジトリもPagesも正常
- 対処: `konta-factory.html` を index.html へのリダイレクト専用ページとして復活（meta refresh ＋ `location.replace()` の二段。`?query`・`#hash` も引き継ぐ）
- 検証: 旧URL **404 → 200** を Pages ビルド完了後に実測。本体URLも 200 のまま
- 学び: **エントリ名を変えたら旧名をリダイレクトとして残す**。フォルダ改名と違い「本体は200」なので気づけない
- 検出: `_tools/check-legacy-entry.sh` で機械検出できるようにした（245で同じ事故が出たのを機に新設）

### 【訂正】上の「404だった」は誤り（2026-09-19 同日中に判明）
- gitで裏を取った結果、`konta-factory.html` は**このリポジトリで一度も公開されていなかった**（`git cat-file -e <修復コミット>^:konta-factory.html` → 不在）。
  改名はローカルフォルダ内で完結しており、リポジトリは改名**後**に作成されている
- つまり `…/242-konta-factory/konta-factory.html` というURLは**元から存在しない**。「配ったリンクが死んだ」という上の記述は**誤り**
- 置いたリダイレクトは**害はないが、壊れていたものを直したわけではない**（将来その名前で来た人を受けるだけの保険）
- 誤認の原因: LEARNINGS.md の本文を証拠として扱ったこと。**本文は作業メモであって証拠ではない。証拠はgit履歴**
- 検出器も v2 で「git履歴に存在 かつ HEADに不在」判定へ作り直した（`_tools/check-legacy-entry.sh`）

---

## 2026-09-20 — ドローン2機化・敵30種・5ステージ・ため技・タイトル/エンディング刷新

### やったこと

1. **攻撃型ドローンを追加**（従来の「ドローン」は実体のないシールド表示だけだった）
2. **敵キャラを30種に量産**。`enemies.js` を新設して型テーブル化
3. **5ステージ構成へ拡張**（外周／工場の中／地下 人体実験場／工場奥の空き地／最新研究室）
4. **ため技**を実装（245-raiken-hikari のホールド式を参考に作り直し）
5. **タイトルをパンク／ディストピア基調へ刷新し、説明文を全撤去**。**エンディングを新規制作**

### 学び1: 「クラスにするか」は規模ではなく必要な口の数で決める

30体を作るにあたって class 階層を組むか迷ったが、**1体に必要な口は `update(e)` と `draw(e,x,y,fl)` の2つだけ**だった。
`new`／`this`／継承を入れると、敵の振る舞いより足場のほうが長くなる。
結果は定義テーブル（`window.ET = {gear:{...}, bat:{...}}`）。敵追加が「1エントリ足す」に収束し、
`hazard` `guard` `onDeath` のような**任意の口**を後から足しても既存29体に一切触らずに済んだ。

判断ルール: **必要な口が2〜3個で、状態を共有しないなら、classより定義テーブル。**

### 学び2: 別ファイル化は「依存を注入する」より「同一グローバルに置く」ほうが安い

`enemies.js` は classic script（`<script src>`）で本体より先に読む。ESモジュールにすると
`p` `GY` `burst()` `move()` を全部エクスポート／インポートする必要があり、敵1体を足すたびに
import 行を触ることになる。同一グローバルスコープなら**定義時ではなく呼び出し時に解決される**ので、
本体側の変数をそのまま参照できる。GitHub Pages 直配信なのでバンドラも不要。

### 学び3（最重要）: レベルの「詰み」は見た目では分からない。物理値を実測してから並べる

5ステージを組んだあと、**右を押しっぱなしにするだけのボット**で通したら、
5面すべてが最初の穴で止まり、合計340回落下した。原因は2つあり、**どちらも目視では気づけなかった**。

1. **穴がジャンプ距離を超えていた**。ジャンプ到達距離を実測すると86.4px。必要距離は「穴幅＋プレイヤー幅16」。
   旧値の穴幅80〜100pxは **96〜116px必要＝平地からは物理的に越えられない**。
   これは原作（2026-09-19版）から引き継いだ値で、1面をずっとそのまま公開していた
2. **穴の踏み切り位置の真上に足場があり、頭がつかえてジャンプが途中で止まっていた**。
   トレースすると `vy` が -5.5 から 0 に落ち、y が足場の底面152で止まっていた。
   **15箇所の穴すべてで発生**（全ステージ全穴）。偶然ではなく、
   「足場は等間隔に散らす／穴も等間隔に空ける」と両者が必ず重なる構造的な事故

対処: 穴幅を54pxへ統一（余裕16px）し、**踏み切り帯（穴の左110px〜右30px）の上の足場を機械的に再配置**。
検査スクリプトで15/15を検出 → 再配置 → 検査0件 → ボット再走で **5/5走破・落下0**。

判断ルール: **ジャンプ距離は推定せず実測する。レベルを並べ終えたら、
「押しっぱなしボット」で通して落下回数を数えるまで完成と呼ばない。**

### 学び4: ホールド技を足すとき、既存のタップの打感を犠牲にしない

参考にした245は「離した瞬間に初めて撃つ」型だが、242はタップ反応の速さをSPECに書いているゲームだった。
そのまま移植すると**全ての通常攻撃に1ホールドぶんの遅延が乗る**。
採った形は二段構え: **押した瞬間に通常ヤリを出し、押し続けた分だけ段が上がり、離すと衝撃波が追加で出る**。
実測で「tap／34f／82f」の3ケースとも通常ヤリ（`p.atk=15`）が発生することを確認した。

### 学び5: DOMのオーバーレイUIは、canvas側の画面遷移を知らない

一時停止ボタンはDOM要素なので、タイトル画面にもエンディングにも出たままだった。
スクリーンショットを撮るまで気づかなかった（ロジックのテストでは絶対に出てこない種類のバグ）。
`state` を見て `display` を切り替え、毎フレームの書き込みを避けるため直前値をキャッシュしている。

判断ルール: **canvas内に状態機械を持つゲームでDOMを重ねたら、画面ごとにスクリーンショットを撮って目で見る。**

### 検証（自己申告ではなく実測）

- 敵30/30が例外なく動作・描画
- ため技3段が想定どおり（lv2=2dmg/w34、lv3=3dmg/w56）
- 攻撃ドローン: 移動0.9px/f、敵中心x=150に対しx=146.4で発射、2発で撃破
- ボット走破: 5/5ゴール到達・落下0回
- 背景5種＋エンディング＋タイトルを実描画して例外0
- harness 14項目 PASS（コンソールエラー0件／0.05MB）

### 2026-09-20 追記: 攻撃ドローン速度1.3倍 ／ ため技エフェクト刷新

- 攻撃ドローン `A_SPEED` を 0.9 → **1.17**（1.3倍）。実測で 1.17px/f、ビーム初弾がフレーム157→**125**へ短縮。2発撃破は維持
- ため技の見た目を作り直し: 三日月の刃＋残像9枚＋外側グロー＋白熱コア。MAXは刃を走る放電と、発生直後11フレームの放射状飛散。後方へ速度線と火花
- 波オブジェクトに `t`（年齢）と `trail`（軌跡）を追加。**形を足すのではなく履歴を持たせると、残像・発生演出・速度線が同じデータから出せる**

#### 学び: テストスクリプトはゲームの仕様変更で静かに壊れる

ドローン速度を測り直したら結果が全部0になった。原因はゲーム側ではなく**テスト側**で、
2つとも今回の改修で入れた変更に追従できていなかった。

1. `anyInput()` 1回で `title→play` だった遷移が `title→intro→play` の2段になり、テストは intro のまま `update()` を回していた（`update` は play 以外で即return するので何も起きない）
2. 撃破された敵を毎フレーム `filter` で配列から外すようにしたため、テストが握っていた `enemies[0]` が途中で `undefined` になって落ちた

判断ルール: **テストが「0件」「null」を返したら、まずゲームではなくテストの前提を疑う。**
とくに①状態機械の遷移段数 ②配列を再代入する処理（filter）は、テスト側が参照を先に握っておく必要がある。

---

## 2026-09-21 — コン太の歩き（足トコトコ・尻尾パタパタ）

### やったこと

歩きを「脚の縦伸び縮み 2px」から、歩幅に同期したアクションへ差し替えた。

- 足: 片足ずつ持ち上げ、上げた足は外側へ踏み出す
- 尻尾: 歩きの gait に合わせて上下へパタパタ。停止は小さく、空中は流す
- エンディングの背中歩きも同じリズムに合わせた

### 検証

- インライン JS の `node --check`: 通過
- プレイ画面で gait 0 / 0.8 / 1.6 / 2.4 / 3.2 のプレイヤー矩形ハッシュが **5種とも異なる**ことを実測
- 拡大描画で gait 1.57（尻尾上・前足接地）と gait 4.71（尻尾横・前足踏み出し）を目視確認
- ページ JS エラー: 0件

### 未検証

- iPhone 実機では未確認
- ジャンプ中の足畳み・攻撃中の歩きはブラウザでポーズ固定確認のみ（実プレイの手触りは未）

---

## 2026-09-21 — 2面ギミック：コンベア床と天井プレス

### やったこと

1面は触らず、2面に地形ギミックを2つ足した。敵の増殖ではなく、副題「ベルトと圧搾機」の実体化。

- **コンベア床** 4本（順流2・逆流2）。接地中だけ ±1.1px/f。穴の踏み切り帯には置いていない
- **天井プレス** (`press`) 3基。破壊不可。予告（床点滅）→叩き下ろし→保持→格納。192f周期

### 検証

- `node --check` index.html / enemies.js 通過
- 置き位置スクリプト: ベルト4本・プレス3基すべて接地、踏み切り帯との重なり0、足場との重なり0
- 順流: 停止状態 x=180 → 約1秒後 x=280.1（ベルト終端まで流された）
- 逆流: 停止状態 x=900 → x=743.8（ベルト始端の左へ押し戻された）
- プレス: 予告の床点滅と、叩き下ろし時のハザード帯付き柱を目視
- ページ JS エラー: 0件

### 未検証

- iPhone 実機
- 5面通しボット。ギミックがあるので右押しっぱなしでは2面で詰まる想定（未走）

---

## 2026-09-21 — 3面ギミック＋全5面ボス扉

### やったこと

3面に培養液と心拍シャッターを足したあと、全面の終端ボスと「倒すと扉が点く」を入れた。

- 培養液 4区画: 接地中だけ歩行 1.15px/f・ジャンプ vy=-6。ダメージなし
- 心拍シャッター 3基: 壁として開閉。接触ダメージなし
- ボス: 004 / 1005 / 模型3号 / 集合体 / 開発者騎乗機。撃破で `bossDown`、ゲートが点灯

### 検証

- 置き位置: 培養液4・シャッター3、踏み切り帯・足場との重なり0
- 培養液上の右移動 vx=1.15 を実測（通常 2.4）
- シャッター閉じ／開きの高さ変化と ECG を目視
- 1面: 錠のゲートを確認 → 004を撃破 → STAGE 2 導入（警備ロボ１００５）へ遷移
- 5面ボス5種を loadStage して描画、例外なし
- `node --check` 通過

### 未検証

- iPhone 実機
- ボス戦のバランス（HPと攻撃間隔の手触り）
- 4面・5面の地形ギミックは次節で着手済み

---

## 2026-09-21 — 4面・5面ギミック

### やったこと

4面に崩れる足場と廃材落下、5面にスキャンライトと床シャットダウンを足した。ボタンは増やしていない。

- **崩れる足場** 4枚（x=300,860,1620,2420）。22fで崩落、200fで復帰
- **廃材落下** (`scrap`) 8箇所。影の予告のあと落下。落下中だけ当たる
- **スキャンライト** 2本。足場の下に隠れると見つからない。22fで下向き弾
- **床シャットダウン** 4箇所・幅48px。180f周期（点灯100／消灯80）

### 検証

- 置き位置スクリプト: 崩落4・廃材8・格子4、すべて接地。踏み切り帯との重なり0。格子と足場の重なり0
- `node --check` index.html / enemies.js 通過
- 崩れる足場: 接地8fで life=8、28f後に gone=194（22fで崩落）。プレイヤーは落下開始
- 廃材: 8体。180f中 hazard 29f、着地中（vy<=0）の hazard は 0
- スキャン: 光に晒すと spot が 1→21、22f目で弾1発・spot=6。足場の下では hiddenUnder が全フレーム true、spot は 8→0 に減衰
- 床シャットダウン: 幅48px、180f中 off が 80f。消灯中に右＋ジャンプで穴を横断、落下0・HP3のまま
- ページ描画: 4面は月と廃材山＋崩落足場。5面は走査光と床の欠落を目視

### 未検証

- iPhone 実機
- 5面通しボット（ギミックがあるので右押しっぱなしでは詰まる想定）
- スキャン弾の回避難度・格子に落ちたときの手触り

---

## 2026-09-21 — 1面背景をマジックアワーに

### やったこと

1面の空が紫一色＋小さな太陽だったので、マジックアワーの帯空に差し替えた。工場は逆光のシルエット。床色も夜紫から暖色の影へ。

### 検証

- `node --check` 通過
- ブラウザで1面を描画: 藍→紫→朱→金の帯、低い太陽、逆光の工場を目視

### 未検証

- iPhone 実機

---

## 2026-09-21 — 1面の空を徐々に変わるグラデに

隣の色が飛びすぎていたので、藍→紫→朱→金を近い色で8段に並べ、`createLinearGradient` で混ぜた。太陽のハロも薄くした。

### 検証

- `node --check` 通過
- ブラウザで1面を描画して目視

### 未検証

- iPhone 実機

---

## 2026-09-21 — コン太のSE（トコトコ／ボーン／えい）

### やったこと

歩き・ジャンプ・ヤリに WebAudio の合成SEを足した。音声ファイルは置いていない。SEを足すのでミュートを同時に入れた。

- トコトコ: 接地歩行中、9フレームおき。左右で高さを変える
- ボーン: ジャンプした瞬間
- えい: ヤリを押した瞬間（ため解放では重ねない）
- ミュート: 右上、`tg.242.mute`

### 検証

- `node --check` 通過
- ミュートボタンの表示と `muted` 切替はブラウザで確認
- iPhone 実機の聞こえ方は未検証

---

## 2026-09-21 — ボスを2倍

### やったこと

5体のボス（004／1005／模型3号／集合体／騎乗機）の `w`/`h` と描画を2倍にした。弾の出る位置も体に合わせた。`mini` はそのまま。

### 検証

- `node --check` 通過
- ブラウザで1面ボスとコン太を並べて目視

### 未検証

- 2〜5面ボスの手触り・弾の当たり

---

## 2026-09-21 — ボス3倍とボスSE

### やったこと

倍率を3倍へ上げ、ボスごとに音を付けた。

- 004／騎乗機: ウイーン
- 1005: がしゃん
- 模型3号: ピピピ
- 集合体: がしゃんがしゃん

画面に入ったときと攻撃時。004・騎乗機・模型3号は待機中も繰り返す。

### 検証

- `node --check` 通過
- ブラウザで1面ボスの大きさを目視。SE関数は例外なく呼べることを確認

### 未検証

- iPhone 実機の聞こえ方
- 2〜5面ボスの手触り

---

## 2026-09-21 — 出荷

1面の空を徐々に変わるマジックアワーにした版を公開。harness 14項目 PASS（0.07MB）。

### 検証

- `_tools/game-harness.sh 242-konta-factory` → RESULT PASS
  証跡: `docs/harness-reports/242-konta-factory-2026-09-21T08-12-26-734Z.md`

### 未検証

- iPhone 実機
- Pages 反映後の本番URL目視（公開後に curl する）
